import { Provider } from '@angular/core';
declare const _default: (change: () => void, resolutions: Map<any, any>) => {
    resolve: (def: any) => any;
    resolveProvider: (def: Provider) => any;
};
export default _default;
