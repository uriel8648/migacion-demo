import { AnyType } from '../../common/core.types';
import { MockedDebugNode } from '../../mock-render/types';
declare type SELECTOR = string | AnyType<any> | [any] | [any, any];
declare const _default: (selector: SELECTOR) => (node: MockedDebugNode) => boolean;
export default _default;
