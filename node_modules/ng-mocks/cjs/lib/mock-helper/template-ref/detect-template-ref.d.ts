import { TemplateRef } from '@angular/core';
import { MockedDebugNode } from '../../mock-render/types';
declare const _default: (result: Array<TemplateRef<any>>, detector: (node: MockedDebugNode) => boolean, limit?: number) => (node: MockedDebugNode) => boolean;
export default _default;
