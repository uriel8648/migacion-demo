import { DebugNode } from '@angular/core';
import { Type } from '../common/core.types';
declare const _default: <T>({ result, el, nodes, normalize, proto, }: {
    el: DebugNode | null;
    nodes: any[];
    normalize: (item: any) => any;
    proto: Type<T>;
    result: T[];
}, gatherDefault: boolean, scanned?: any[]) => void;
export default _default;
