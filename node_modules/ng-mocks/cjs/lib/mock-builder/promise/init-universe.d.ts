import { BuilderData } from './types';
declare const _default: ({ configDef, defProviders, defValue, excludeDef, keepDef, mockDef, replaceDef, }: BuilderData) => Map<any, any>;
export default _default;
